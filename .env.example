# Für lokale Entwicklung: Kopie als .env.local anlegen (wird von Next.js gelesen).
# .dev.vars wird nur von Wrangler/Cloudflare genutzt – für "pnpm dev" (Next.js) braucht der
# Client NEXT_PUBLIC_GOOGLE_MAPS_API_KEY hier in .env.local.

NEXT_PUBLIC_GOOGLE_MAPS_API_KEY=dein-google-maps-api-key
GOOGLE_MAPS_API_KEY=dein-google-maps-api-key
